# Archivo de configuración para Windows
# Copia este archivo como .env en el directorio de instalación

# Servidor web
SERVER_HOST=localhost
SERVER_PORT=8000

# Logging
LOG_LEVEL=INFO

# Base de datos
# La aplicación crea automáticamente la base de datos SQLite
# Si necesitas una ubicación específica, descomenta y modifica:
# DATABASE_URL=sqlite:///C:/Users/%USERNAME%/AppData/Local/CambioDollar/cambio_dollar.db

# Scheduler (capturas automáticas)
SCHEDULER_INTERVAL_SECONDS=300  # 5 minutos

# Configuración de proveedores
# La aplicación incluye configuración por defecto para proveedores Dominicanos
# Para personalizar, modifica los proveedores en el código fuente

# Tema de la interfaz (light/dark)
# THEME=dark